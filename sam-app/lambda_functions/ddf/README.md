# Nested Stack for Lambda Functions - ddf Specific Sources

This folder contains Lambda functions specifically designed for ddf data sources. The functions are listed in the order in which they are utilized within the state machines for streamlined workflow orchestration.

# Overview

Each Lambda function in this folder plays a critical role in the processing pipeline for ddf-specific sources, ensuring efficient validation, data download, and synchronization.

# Lambda Functions Order

The functions are organized to match the execution sequence within the state machines, facilitating a clear understanding of their usage and dependencies.